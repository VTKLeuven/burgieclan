version: '3.5'

services:
  db:
    image: mysql:8.0
    volumes:
      - easyadmin-db:/var/lib/mysql
    ports:
      - '3306:3306'
    environment:
      MYSQL_ROOT_PASSWORD: burgiclan_db_root
      MYSQL_DATABASE: burgiclan_db
      MYSQL_USER: burgiclan_db_user
      MYSQL_PASSWORD: burgiclan_db_pass

  mailcatcher:
    image: schickling/mailcatcher
    ports:
      - '1025:1025'
      - '1080:1080'

volumes:
  easyadmin-db: {}