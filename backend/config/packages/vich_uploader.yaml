vich_uploader:
    db_driver: orm
    metadata:
        type: attribute
    mappings:
        document_object:
            uri_prefix: /files/download
            upload_destination: '%kernel.project_dir%/data/documents'
            namer:
                service: Vich\UploaderBundle\Naming\SmartUniqueNamer
                options: { keep_extension: true }

# Production: use Flysystem (S3) storage instead of local filesystem
when@prod:
    vich_uploader:
        storage: flysystem
        mappings:
            document_object:
                uri_prefix: /files/download
                upload_destination: default.storage
                namer:
                    service: Vich\UploaderBundle\Naming\SmartUniqueNamer
                    options: { keep_extension: true }
